/**
 * Generated by orval v8.5.3 🍺
 * Do not edit manually.
 * Api
 * Online Chess API
 * OpenAPI spec version: 0.1.0
 */
export interface HealthStatus {
  status: string;
}

export interface OkResponse {
  ok: boolean;
}

export interface ErrorResponse {
  error: string;
}

export interface LoginRequest {
  username: string;
  password: string;
}

/**
 * The chess color assigned to this user
 */
export type AuthSessionColor =
  (typeof AuthSessionColor)[keyof typeof AuthSessionColor];

export const AuthSessionColor = {
  white: "white",
  black: "black",
} as const;

export interface AuthSession {
  username: string;
  /** The chess color assigned to this user */
  color: AuthSessionColor;
}

/**
 * Promotion piece if pawn reaches last rank
 */
export type MoveRequestPromotion =
  (typeof MoveRequestPromotion)[keyof typeof MoveRequestPromotion];

export const MoveRequestPromotion = {
  q: "q",
  r: "r",
  b: "b",
  n: "n",
} as const;

export interface MoveRequest {
  /** Source square in algebraic notation (e.g. e2) */
  from: string;
  /** Destination square in algebraic notation (e.g. e4) */
  to: string;
  /** Promotion piece if pawn reaches last rank */
  promotion?: MoveRequestPromotion;
}

export type PlayerInfoColor =
  (typeof PlayerInfoColor)[keyof typeof PlayerInfoColor];

export const PlayerInfoColor = {
  white: "white",
  black: "black",
} as const;

export interface PlayerInfo {
  username: string;
  color: PlayerInfoColor;
  connected: boolean;
}

export type MoveRecordColor =
  (typeof MoveRecordColor)[keyof typeof MoveRecordColor];

export const MoveRecordColor = {
  white: "white",
  black: "black",
} as const;

export interface MoveRecord {
  from: string;
  to: string;
  /** Standard algebraic notation of the move */
  san: string;
  color: MoveRecordColor;
  promotion?: string;
}

export interface TimeControl {
  /**
   * Initial clock per side in seconds
   * @minimum 0
   */
  initialSeconds: number;
  /**
   * Fischer increment added after each move (seconds)
   * @minimum 0
   */
  incrementSeconds: number;
}

export interface ResetGameRequest {
  /** Optional time control. Null or omitted means no clock. */
  timeControl?: TimeControl | null;
}

export type GameStateTurn = (typeof GameStateTurn)[keyof typeof GameStateTurn];

export const GameStateTurn = {
  white: "white",
  black: "black",
} as const;

/**
 * Current status of the game
 */
export type GameStateStatus =
  (typeof GameStateStatus)[keyof typeof GameStateStatus];

export const GameStateStatus = {
  waiting: "waiting",
  active: "active",
  check: "check",
  checkmate: "checkmate",
  stalemate: "stalemate",
  draw: "draw",
  timeout: "timeout",
} as const;

export type GameStateWinner =
  | (typeof GameStateWinner)[keyof typeof GameStateWinner]
  | null;

export const GameStateWinner = {
  white: "white",
  black: "black",
  draw: "draw",
} as const;

/**
 * Which side's clock is currently ticking (null = paused)
 */
export type GameStateTickingFor =
  | (typeof GameStateTickingFor)[keyof typeof GameStateTickingFor]
  | null;

export const GameStateTickingFor = {
  white: "white",
  black: "black",
} as const;

export interface GameState {
  /** FEN string describing the current position */
  fen: string;
  turn: GameStateTurn;
  /** Current status of the game */
  status: GameStateStatus;
  winner?: GameStateWinner;
  white?: PlayerInfo | null;
  black?: PlayerInfo | null;
  moves: MoveRecord[];
  /** Pieces captured by white (lowercase letters) */
  capturedByWhite: string[];
  /** Pieces captured by black (uppercase letters) */
  capturedByBlack: string[];
  lastMoveAt?: string | null;
  /** Monotonic version for clients to detect updates */
  version: number;
  /** Active time control (null = unlimited / no clock) */
  timeControl?: TimeControl | null;
  /** White's remaining time in ms (server snapshot) */
  whiteTimeMs: number;
  /** Black's remaining time in ms (server snapshot) */
  blackTimeMs: number;
  /** Which side's clock is currently ticking (null = paused) */
  tickingFor?: GameStateTickingFor;
  /** Server's current timestamp; clients use this to extrapolate ticking time */
  serverNow: string;
}
